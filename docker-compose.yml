version: '3.3'
services:
  backend:
    build: .
    container_name: cryptax_backend
    hostname: cryptax
    ports:
      - '8080:8080'
    environment:
        - PROFILE=${PROFILE}
        - JASYPT_PASSWORD=${JASYPT_PASSWORD}
