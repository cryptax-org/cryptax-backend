apply plugin: 'java-library'

ext.moduleName = 'cryptax.id'

dependencies {
	implementation(
		project(':domain'),
		[group: 'com.fasterxml.uuid', name: 'java-uuid-generator', version: '3.1.5'],
	)
}
